# Run 3
- **ケース**: TC-02
- **モード**: スキルあり（発動判定は description のみで実施。本文・references は不参照）
- **実行日時**: 2026-07-11 13:30
- **手順の記録**:
  1. `plugins/bitz-sdd/skills/sdd-data/SKILL.md` の frontmatter description **のみ**を読み、4つの発言に対して「sdd-data を使うか」を判定した（Discovery 段階の再現）。
  2. (a)「タスクの保存先を SQLite にするか JSON ファイルにするか決めたい」→ **使う**。description のトリガー「格納方式」に直接該当し、守備範囲（RDB / ファイル比較選定）の中心事例。
  3. (b)「テーブル設計をレビューして」→ **使う**。トリガー「テーブル設計」「スキーマ設計」に該当。ただしレビュー工程自体は sdd-review の管轄であり、実運用では sdd-data（設計知識の提供）+ sdd-review（判定）の併用になる点を備考に記録。
  4. (c)「標準入力を整形して標準出力に出すだけの CLI フィルタを設計して。状態は持たない」→ **使わない**。description 末尾の「データを永続化しないシステムでは不要（必須工程ではない）」に明示的に該当。
  5. (d)「UI の配色を決めたい」→ **使わない**。データ格納に関するトリガー語がひとつも該当しない。
- **成果物**: なし（判定のみ）
- **備考**: description 単体で4件とも迷いなく判定できた。(b) は「レビュー」という語で sdd-review と競合しうるが、description に「テーブル設計」が明示されているため発動自体は正しく起きる。
