# TC-02 スキルあり回答（env-orchestration の指示に従う）

**入力プロンプト**: この設計変更、advisor に相談してから決めたい

## 前提の読み込み
- `.claude/bitz-env.local.md`（レジストリ）は存在しない
- `.claude/agents/` を確認 → `worker.md` はあるが `advisor.md` は存在しない

## パターン選択の決定木の適用
- 設計変更 = 「深い判断が必要」に該当。影響の大きさ次第で相談型 or 合議型だが、
  今回は「相談したい」という指示なので相談型として扱う
- 決定木: 【相談型】advisor へ。**advisor 不在なら中心が判断し、その旨を明記**

## 劣化動作の適用
advisor が不在（.claude/agents/advisor.md なし、レジストリにも advisor 登録なし）のため、
「相談スキップ」の劣化動作を適用する。停止せず、中心（自分）が設計変更について判断し、
advisor 不在で相談をスキップした旨を成果物に明記する。

## 判断（例）
（ここでは実際の設計変更の詳細が示されていないため、判断のプレースホルダーとして
「advisor 不在のため中心が暫定判断を行い、後日 advisor が用意され次第レビューを依頼する」
という進め方を採用）

## 判断記録
- advisor 不在（.claude/agents/advisor.md なし、レジストリなし）を検出した
- 相談をスキップして中心が判断を続行した（停止していない）
- **スキップした旨をこの回答（成果物）に明記済み**: 「advisor 不在のため相談スキップ、
  中心が暫定判断」
