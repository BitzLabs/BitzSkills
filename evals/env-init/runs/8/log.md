# TC-04 実行ログ（発動判定・使うべき）

- **ケースID**: TC-04（発動判定：使うべき）
- **モード**: 発動判定（SKILL.md の frontmatter `description` のみを読み、本文は読まない）
- **実行日時**: 2026-07-12

## 読んだ範囲
`plugins/bitz-env/skills/env-init/SKILL.md` の frontmatter のみ:

> bitz-env の開発環境（ガードレール permissions・AGENTS.md/CLAUDE.md 雛形・advisor/worker
> サブエージェント）を対象プロジェクトへ対話的に展開する。「開発環境を展開して」
> 「環境をセットアップして」「ガードレールを入れて」「協調環境を初期化して」「env-init」
> と言われたとき、または bitz-env 導入直後の初期設定時に使用する。展開済み環境の診断は
> env-doctor、協調アダプタの登録は env-register が担当する。

本文（ワークフロー等）は読んでいない。ファイル操作は行っていない。

## 判定

### (a) 「新しいリポジトリにいつものガードレールと協調環境をセットアップしたい」
- **判定**: 使う ✅
- **理由**: description のトリガー表現「環境をセットアップして」「ガードレールを入れて」
  「協調環境を初期化して」のいずれとも語彙・意味が強く一致する
  （「ガードレール」「協調環境」「セットアップ」の3語すべてが揃っている）。
  「新しいリポジトリ」は「bitz-env 導入直後の初期設定時」という利用条件にも合致する。

### (b) 「env-init 走らせて」
- **判定**: 使う ✅
- **理由**: description のトリガー表現に「env-init」というスキル名そのものが明記されており、
  文字通りの直接呼び出しに該当する。曖昧さはない。

## アサーション結果
- [x] (a) を「このスキルを使う」と判定した → ✅
- [x] (b) を「このスキルを使う」と判定した → ✅

## 備考
- 両ケースとも description 中のトリガー語句とほぼ字面レベルで一致しており、
  判定に迷いはなかった。発動判定の精度としては良好。
