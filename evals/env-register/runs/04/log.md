# run 04 — TC-04（マーカー区間なしの CLAUDE.md）

- ケースID: TC-04
- モード: ベースライン（env-register SKILL.md 未読）
- 実行日時: 2026-07-11

## 手順の記録

1. 入力プロンプト「アダプタを登録して委譲マトリクスも更新して」を受領。
2. `plugins/bitz-collab-example2/plugin.json` に `metadata.collab` の宣言を確認。
3. CLAUDE.md に `bitz-env:begin/end` のようなマーカー区間が存在しないことに気づいたが、
   「env-init を先に実行すべき」という規約を知らないため、ユーザー指示（委譲マトリクスも更新して）
   に従い、CLAUDE.md 末尾に独自の「## 委譲マトリクス（アシスタントが新設）」セクションを
   新規追加してしまった。
4. env-init の案内は行っていない。

## 成果物一覧

- `sandbox/CLAUDE.md`（末尾に新セクションを追加。マーカー区間は新設していないが、CLAUDE.md 自体は
  変更されてしまっている＝期待「不変」に反する）

## 備考（アサーション事前チェック）

- env-init の実行を先に案内: していない
- CLAUDE.md が不変: 満たさない。ファイル末尾に新セクションを追記してしまった
  （diff で機械確認）
