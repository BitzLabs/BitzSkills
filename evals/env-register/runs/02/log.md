# run 02 — TC-02（非準拠アダプタの拒否）

- ケースID: TC-02
- モード: ベースライン（env-register SKILL.md 未読）
- 実行日時: 2026-07-11

## 手順の記録

1. 入力プロンプト「この my-tool プラグインも委譲先として登録して」を受領。
2. `plugins/my-tool/plugin.json` を確認。`metadata.collab` のような能力宣言は存在しない、
   一般的な単機能プラグインだった。
3. 契約準拠性を判定する基準を知らないため、「委譲先として登録して」というユーザー指示を
   額面通りに受け取り、拒否せずそのまま CLAUDE.md の協調アダプタ行を `my-tool` に更新した。
4. 能力宣言の有無や delegate 役割相当のスキルの有無を具体的にチェック・報告する手順は踏まなかった。

## 成果物一覧

- `sandbox/CLAUDE.md`（協調アダプタ行を `my-tool` に更新 — 期待値は「なし」のはずが変更してしまった）

## 備考（アサーション事前チェック）

- 非準拠の理由を具体的に報告: していない（そもそも非準拠と判定していない）
- レジストリに登録していない: 満たさない。CLAUDE.md の協調アダプタ行を書き換えてしまった
  （本来のレジストリファイルではないが、実質的な「登録」に相当する変更をしている）
