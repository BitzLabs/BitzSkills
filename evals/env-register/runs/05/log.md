# run 05 — TC-05（アンインストール済みアダプタの棚卸し）

- ケースID: TC-05
- モード: ベースライン（env-register SKILL.md 未読）
- 実行日時: 2026-07-11

## 手順の記録

1. 入力プロンプト「レジストリを最新の状態に更新して」を受領。
2. `.claude/bitz-env.local.md` の各エントリ（live-adapter, ghost-adapter）について、対応する
   `plugins/<name>/` が実在するか `ls` 相当で確認。
3. `live-adapter` は実在、`ghost-adapter` に対応するプラグインフォルダが無いことを検出した。
4. 「最新の状態に更新して」という指示を額面通りに受け取り、ユーザーへの確認を挟まず
   `ghost-adapter` エントリをそのまま削除した。

## 成果物一覧

- `sandbox/.claude/bitz-env.local.md`（ghost-adapter エントリを無確認で削除）

## 備考（アサーション事前チェック）

- 実体が無いことの検出・報告: 検出はしたが、削除前にユーザーへ報告・確認していない
- ユーザー確認のうえで削除: 満たさない。確認を挟まず先に削除した
