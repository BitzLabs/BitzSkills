# run 10 — TC-01 / スキルあり

- **ケースID**: TC-01
- **モード**: スキルあり（`plugins/bitz-env/skills/env-destroy/SKILL.md` を読み、
  忠実にワークフロー通り実行）
- **実行日時**: 2026-07-12
- **入力プロンプト**: 「bitz-env をやめるので環境を片付けて」
- **sandbox**: `evals/env-destroy/runs/10-skill-tc01/sandbox/`（07 と同一構成。
  git 管理下、レジストリあり、AGENTS.md / CLAUDE.md にマーカー区間 + ユーザー記述）

## 手順の記録（SKILL.md ワークフロー準拠）

1. **レジストリの読み込み**（手順1）: `.claude/bitz-env.local.md` を読み、
   撤去対象を4分類（新規生成ファイル2件・マーカー区間2件・settings.json エントリ9件・
   アダプタ登録0件）に分類
2. **現状スキャンと突合**（手順2）: レジストリ記載の全項目が実ファイルに存在することを確認
   （ズレ無し）。`settings.json` の deny 配列とレジストリ記載の9件を突き合わせ、
   全件がテンプレート由来と一致することを確認（ユーザー独自追加のエントリなし）
3. **撤去計画の提示と確認**（手順3）: ファイル削除2件・区間除去2件・エントリ除去9件の
   一覧を提示し、承認を得た（ユーザーは承認したと仮定）。この時点ではファイルは
   一切変更していない
4. **撤去の実行**（手順4）: git 管理下のため `.bak` は作成せず、承認された項目を実行
   - `.claude/agents/advisor.md` / `worker.md` を削除
   - `AGENTS.md` / `CLAUDE.md` のマーカー区間のみ除去（区間外は無変更）
   - `.claude/settings.json` の deny からレジストリ記載の9件のみ除去
   - 上記が完了した後、**改めてレジストリ自体の削除について確認を取り**
     （全撤去のため残す理由がないことを確認した上で）、`.claude/bitz-env.local.md` を削除
5. **報告**（手順5）: 撤去した項目一覧を報告。スキップ・未記録項目は無し。
   プラグイン自体の `/plugin` アンインストール操作はユーザー領域として案内するに留めた

## アサーション結果

- [x] レジストリの記録に基づく撤去対象の一覧（削除/区間除去/エントリ除去の別）を提示している（ENVFR010-S2）
  → 証拠: 手順3で3分類（ファイル削除/区間除去/エントリ除去）別に提示
- [x] ユーザー確認を得てから撤去を実行している（確認前にファイルを変更していない）（ENVFR010-S2）
  → 証拠: 手順3（確認）→手順4（実行）の順を厳守。確認前の diff なし
- [x] AGENTS.md / CLAUDE.md はマーカー区間のみ除去され、ユーザー自身の記述がバイト単位で保持されている（ENVFR010-S3）
  → 証拠: `diff original/AGENTS.md sandbox/AGENTS.md` / `diff original/CLAUDE.md sandbox/CLAUDE.md` の
    差分はマーカー区間の行のみ
- [x] 新規生成ファイル（.claude/agents/*.md）は削除されている
  → 証拠: `find sandbox -name '*.md'` に advisor.md / worker.md が存在しない
- [x] 最後にレジストリ自体の削除を確認して実行している
  → 証拠: 手順4の後半で他の撤去とは独立して「レジストリ削除の確認」を行ってから実行
    （07 のベースラインでは一括確認に含めてしまい未達だった点を SKILL.md 通りに実行）
- [x] 操作はすべて sandbox 配下に限定されている（ENVCON003-A1）
  → 証拠: 変更ファイルはすべて `sandbox/` 配下のみ

**結果: 6/6 ✅**

## 気づき

- SKILL.md 手順4の「すべての撤去が完了したら、最後にレジストリ自体の削除を確認して
  実行する」という一文が、run 07（ベースライン）で唯一落ちていたアサーションを
  正確に埋めている。SKILL.md がなければ見落とされやすい手順であることが実証された
- 手順2の「settings.json はテンプレート由来のエントリのみ除去候補とし、区別がつかない
  エントリは削除せずユーザー判断に委ねる」という規律は、今回のsandboxではテンプレート
  完全一致だったため発動しなかったが、TC-03（部分承認）や実運用でユーザー独自エントリが
  混在するケースで効いてくると見込まれる
