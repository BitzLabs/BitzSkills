# run 12 — TC-03 / スキルあり

- **ケースID**: TC-03
- **モード**: スキルあり（`plugins/bitz-env/skills/env-destroy/SKILL.md` を読み、
  忠実にワークフロー通り実行）
- **実行日時**: 2026-07-12
- **入力プロンプト**: 「片付けたいけど AGENTS.md のガードレール節だけは残して」
- **sandbox**: `evals/env-destroy/runs/12-skill-tc03/sandbox/`（TC-01 と同一構成。
  git 管理下、レジストリあり）

## 手順の記録（SKILL.md ワークフロー準拠）

1. **レジストリの読み込み**（手順1）: 撤去対象を4分類に整理
2. **現状スキャンと突合**（手順2）: レジストリと実体の間にズレ無しを確認
3. **撤去計画の提示と確認**（手順3、部分承認）: 全撤去候補を提示した上で、
   ユーザーの「AGENTS.md のガードレール節だけは残して」という部分承認を反映し、
   `AGENTS.md` のマーカー区間のみ撤去対象から外した計画を確定
4. **撤去の実行**（手順4）:
   - `.claude/agents/advisor.md` / `worker.md` を削除
   - `CLAUDE.md` のマーカー区間のみ除去（`AGENTS.md` には一切触れない）
   - `.claude/settings.json` の deny からレジストリ記載9件を除去
   - SKILL.md 手順4「部分撤去の場合は残し、撤去済み項目を記録から消す」に従い、
     レジストリ **`.claude/bitz-env.local.md` 自体は削除せず**、
     撤去済み3項目に取り消し線を付けて「撤去済み」と記録し、
     `AGENTS.md` のマーカー区間エントリのみ「残置」として明記した形に更新
5. **報告**（手順5）: 撤去した項目・残した項目（AGENTS.md のガードレール節）・
   更新後のレジストリの状態を報告

## アサーション結果

- [x] 承認された項目のみ撤去し、AGENTS.md のマーカー区間は残している
  → 証拠: `diff original/AGENTS.md sandbox/AGENTS.md` の差分ゼロ（完全一致）。
    `.claude/agents/advisor.md`・`worker.md`・CLAUDE.md のマーカー区間・
    settings.json の deny 追記は撤去済み
- [x] 部分撤去のためレジストリを削除せず、撤去済み項目だけ記録から消している
  → 証拠: `.claude/bitz-env.local.md` は削除されず残存（`ls` で確認）。
    内容は撤去済み3項目に取り消し線、`AGENTS.md` の残置1件のみ有効エントリとして更新

**結果: 2/2 ✅**

## 気づき

- run 09（ベースライン）で唯一落ちた「部分撤去時のレジストリ扱い」を、
  SKILL.md 手順4の「部分撤去の場合は残し、撤去済み項目を記録から消す」という
  一文が正確にカバーした。ベースラインは全撤去（TC-01）のパターンをそのまま
  部分撤去に流用してレジストリごと消してしまったが、スキルは部分撤去を
  独立したケースとして扱っている
- レジストリの更新表現（取り消し線 + 残置注記）は SKILL.md に厳密な書式指定が
  無いため、今回は可読性重視で独自に構成した。将来的にレジストリの更新書式を
  明文化する余地がある（気づきとして記録するのみで、本タスクでは修正しない）
