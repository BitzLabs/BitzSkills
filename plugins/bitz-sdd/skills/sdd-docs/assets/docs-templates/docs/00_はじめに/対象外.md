---
id: DOC-context-non-goals
title: 対象外
status: active
version: 0.1.0
changeImpact: low
project_type: app            # app | library
updated: 2026-07-07
owner: <担当ハンドル>
superseded_by: null
---

<!--
  「意図的にやらないこと」を明文化する文書。
  library では特に重要 — スコープ境界が API 表面積と互換性負債を直接決める。
  「今はやらない（後で検討）」と「原則やらない（設計思想）」を分けて書く。
-->

# 対象外

## 原則やらない（設計上の境界）

<!--
  library: この境界を越える要望は API に足さず、拡張点・別 crate/パッケージへ誘導する。
           越えると SemVer 上の互換性負債が恒久化するため、ここは強い契約。
  app:     プロダクトが担わない領域。他システム/手作業/外部サービスに委ねる範囲。
-->
- **<やらないこと>** — 理由: <なぜ境界の外か>。代替: <利用者はどうすべきか>
- **<やらないこと>** — 理由: <...>。代替: <...>

## 今はやらない（保留・時期尚早）

<!-- 将来 .spec/ の要件として起票され得るもの。ここは「保留」の記録。 -->
- **<保留項目>** — 条件: <どうなったら再検討するか>
- **<保留項目>** — 条件: <...>

## よくある誤解（これは Non-Goal です）

<!--
  library では「できると思われがちだが対象外」を明示すると Issue が減る。
  例: 「このライブラリはランタイムの X も面倒を見てくれる」→ No、それは呼び出し側の責務。
-->
- <誤解されやすい期待> → 実際は <対象外である旨>
