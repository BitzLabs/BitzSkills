{
  "perspective": "data-integrity",
  "score": 2.3,
  "summary": "FLW-DSN-016 は guard 直列化の中核（canonical 昇順 acquire・index 包含規約・create 時 nonce による instance 識別）を正しく設計しており、多重語表と fixture 区分割当は実際に数えても正確である。しかし状態空間の完全性には検算で確認できる穴が残る。§8 が意図的に選んだ step 順序（registry → dir → local-ref）が生む中断状態は、§6 の解除区分 4 種のどれにも当てはまらない（三者の存在パターン 8 通りのうち分類があるのは 3 通りのみ）。§6 が最頻ケースを救うために新設した `worktree-registry-stale` は、必須証跡として「非存在証明」を要求しており、§8 の ABA 節が自ら『否定的主張は観測から証明できない』と結論した認識論と真っ向から矛盾する。加えて `worktree_state` は作業状態・PR 状態・merge 状態という直交軸を単一 enum へ潰しているため dirty と MERGED_EXACT が同時成立し、finish（＝削除）側に dirty fixture が無いことで、SI-FLW-046 が閉じたはずの『復元できない未コミット作業の喪失』が finish 経路に残っている。",
  "findings": [
    {
      "id": "DIN-001",
      "severity": "critical",
      "title": "§8 が選んだ step 順序が生む中断状態が §6 のどの解除区分にも落ちず、順序選択の根拠が §6 表自身によって反証されている",
      "detail": "§6 の解除区分は directory / registry entry / local-ref の三者を存在軸として分類する（`worktree-no-effect` は三者とも不在、`worktree-residue-retained` は『directory だけ残存。registry entry と ref は不在』、`worktree-registry-stale` は『registry entry だけ残存』、`worktree-unresolved` は『三者が矛盾』）。三者の存在パターンは 2^3 = 8 通りだが、分類が与えられているのは (0,0,0) / (1,0,0) / (0,1,0) の 3 通りだけである。残る 5 通り（(0,0,1) local-ref だけ残存、(1,1,0)、(1,0,1)、(0,1,1)、(1,1,1)）はどの行にも該当しない。`worktree-unresolved` の定義は『三者が矛盾（例: registry entry と実体が別 instance を指す）』であり『上記以外すべて』ではないため、包括区分としても機能しない。\n\nこれは机上の組合せではなく、本書が §8 の step 契約で**意図的に選んだ順序が必ず通る状態**である。discard は `freeze-manifest` → `verify-manifest-scope` → `remove-registry-entry` → `remove-worktree-dir` → `delete-local-branch`、finish も同じ 3 step を同順で持つ。したがって中断点は次の 3 つになる。\n\n| 中断点 | (dir, registry, ref) | §6 の該当区分 |\n|---|---|---|\n| registry 削除後・dir 削除前 | (1, 0, 1) | **なし** |\n| dir 削除後・ref 削除前 | (0, 0, 1) | **なし** |\n| ref 削除後 | (0, 0, 0) | `worktree-no-effect`（DIN-004 の識別不能問題） |\n\n§8 は `remove-registry-entry` を先に置く根拠として『registry を先に消せば、残った実体は「registry 外の孤立ディレクトリ」として §6 の `worktree-residue-retained` へ**一意に確定できる**』と書いている。しかし `worktree-residue-retained` の必須証跡は『registry entry **と ref は不在**』であり、この中断点では local-ref がまだ存在するため成立しない。**設計が順序を選んだ根拠そのものが、同じ文書の §6 表によって反証されている。** M2-FLT-034（worktree guard を quarantine へ落とす → §6 の3区分へ一意に確定）は、この最も起きやすい中断点を注入すれば FAIL する。結果として、GP-005 が求めた『正規の解放経路』が discard / finish の全中断点で存在せず、回復可能な残渣が恒久 quarantine に固定される。",
      "recommendation": "§6 の区分表を三者（dir / registry / local-ref）の存在パターン 8 通りをすべて覆う決定表へ書き換える。最低限、`worktree-ref-only-residue`（ref だけ残存 — delete-local-branch 未実行）と `worktree-dir-and-ref-residue`（registry のみ除去済み — §8 が選んだ順序の第一中断点）を独立区分として新設し、それぞれの必須証跡と解除可否を定義する。併せて `worktree-unresolved` の定義を『矛盾』から『上記いずれにも一意化できない場合』へ改め、区分表を全域関数にする。fixture は M2-FLT-034 を 3 中断点それぞれへ分割し、『どの中断点でも §6 のちょうど1区分へ落ちる』を完了条件にする。"
    },
    {
      "id": "DIN-002",
      "severity": "critical",
      "title": "worktree_state が直交する 3 軸を単一 enum へ潰しており、dirty × MERGED_EXACT の同時成立で finish が未コミット作業ごと削除しうる",
      "detail": "§2 の `worktree_state` は `ABSENT, ACTIVE_CLEAN, ACTIVE_DIRTY, PR_OPEN, MERGED_EXACT, REMOTE_ADVANCED, WORKTREE_MISMATCH, ORPHAN, FAILED_RETAINED` の 9 値を単一 field の閉集合とする。しかし判定条件（FLW-DSN-006「audit分類」表）を見ると、これらは**独立した 3 軸**である。(a) worktree の作業状態（clean / dirty）、(b) PR の状態（open / merged）、(c) remote ref の進行（remote-advanced）。`ACTIVE_DIRTY` の条件は『登録一致・変更あり』、`PR_OPEN` の条件は『head一致のopen PR』、`MERGED_EXACT` の条件は『merged PR headとbranch SHA一致』であり、**3 つとも同時に真になりうる**（PR を開いたまま編集を続ける、merge 後にローカルで作業を再開する、はいずれも常用パターンである）。\n\n単一 enum である以上どれか1つを返さねばならないが、**優先順位はどこにも定義されていない。** `FLW-DSN-012` は『単一の証拠で複数状態に該当する場合は、より危険側の状態を採用し `BLOCKED` にする』と規定するが、`worktree_state` 9 値の危険度順序は §2 にも §8 にも無い。結果は次の 2 つのどちらかで、いずれも不良である。\n\n1. 規定どおり `BLOCKED` に倒す → **PR を開いたまま作業を続けるという正常状態が恒常的に BLOCKED になる**（§4 が guard 前倒し案を却下した理由と同じ運用破綻）。\n2. 実装が任意の優先順位を選ぶ → 分類が非決定になり、`FLW-FR-007`『証跡が競合する THEN 分類を推測せず INDETERMINATE』に反する。\n\nさらに深刻なのは finish 経路である。`MERGED_EXACT` は `FLW-DSN-012` 写像表で `audited` → `worktree.finish` の許可前提であり、finish の step は `remove-worktree-dir` を含む。dirty かつ merged の worktree が `MERGED_EXACT` へ分類されれば、**未コミットの作業を含む directory がそのまま削除される。** §9 の dirty 系 fixture は `M2-FLT-031`（**discard** の dirty → 退避要求）1 件だけで、**finish の dirty fixture は存在しない**。§8 の recovery matrix でも finish 行は『merge 証跡不足 → BLOCKED』『step 境界で中断 → PARTIAL』の 2 行のみで dirty を扱わない。本書は §5 で SYN-004 について『失うものは remote branch と違って**復元できない**』と正しく認識しているが、同じ損失が finish 経路に無防備で残っている。",
      "recommendation": "(1) `worktree_state` を直交軸へ分解する（`worktree_content_state: CLEAN/DIRTY` と `worktree_lifecycle_state: ABSENT/ACTIVE/PR_OPEN/MERGED_EXACT/REMOTE_ADVANCED/MISMATCH/ORPHAN` 等）か、単一 enum を維持するなら §2 へ**危険度の全順序**を明示し、同時成立時にどの値へ倒すかを決定表で固定する。(2) `worktree.finish` の precondition へ『対象 worktree が clean であること』を追加し、dirty なら `M2-FLT-031` と同じ退避要求＋`BLOCKED` にする。(3) fixture へ `dirty かつ MERGED_EXACT の worktree へ finish 要求`（期待結果: 退避なしの apply を BLOCKED、削除 0 件）を追加し、M2-5 へ割り当てる。"
    },
    {
      "id": "DIN-003",
      "severity": "critical",
      "title": "worktree.finish の sync-main step が guard 集合に含まれない target（main の branch ref・index）を変更し、GP-003 が塞いだ穴と同型の穴が残っている",
      "detail": "operation catalog（§1 の表）は `worktree.finish` の canonical mutation target を『`worktree-dir` ＋ `worktree-registry` ＋ `local-ref` ＋ `index`』とする。ここでの `index` は §3 の包含規約が定める『**W に属する** index target』（`canonical_index_target(common_dir, worktree_id)`）であり、対象 worktree W のものである。\n\nところが §8 の step 契約は finish に `sync-main` を含めている。この step が変更するのは main worktree の branch ref、main worktree の index、および remote-tracking ref 集合であり（`FLW-DSN-015` の `git.sync` の canonical mutation target がまさにこの 3 種）、**いずれも finish の guard 集合に入っていない**。\n\nこれは `FLW-REV-011:SYN-003`（P0）が指摘し §3 の包含規約が塞いだ穴と**構造的に同一**である。SYN-003 は『discard が3者を保持していても別 operation が index key だけを取って stage を実行でき、key 集合が交差しないため双方とも BLOCKED にならない』というものだった。finish の場合は逆向きで、finish 自身が main の ref / index を握らずに変更するため、同時に走る `git.sync` や `git.commit`（main 側）と **key 集合が交差せず、両方が同時に main の branch ref を書く**。M1 が `git.sync` に対して『branch ref、index、remote-tracking ref 集合』の複数 target guard を必須としたのは、まさにこの競合を防ぐためである。finish は同じ副作用を guard なしで実行することになる。\n\nなお §3 の包含規約は『worktree 系 kind が含まれるなら**対応する** index target を自動付加する』と規定しており、この自動付加は W の index に限られる。main の index は自動付加の対象外なので、実装の正規化でも救われない。",
      "recommendation": "(1) `sync-main` を `worktree.finish` の step から外し、独立した `git.sync` operation として前段に置く（`FLW-CON-006` の自動連結禁止と同じ扱い。finish の precondition に『main が最新であること』を置き、満たさなければ `BLOCKED`）。これが最も本書の設計原則に一致する。(2) step として残すなら、operation catalog の `worktree.finish` 行の canonical mutation target へ main の `local-ref` / `index` / `remote-tracking-ref` を明記し、canonical 昇順 acquire の対象へ加える。ただしこの場合 finish が main を長時間 guard することになるため、(1) を推奨する。(3) fixture へ『finish 実行中に別 process が main へ commit / sync』を追加する。"
    },
    {
      "id": "DIN-004",
      "severity": "critical",
      "title": "worktree-no-effect が「discard 成功」と観測上区別できず、M1 の confirmed-done に相当する区分が worktree 版に存在しない",
      "detail": "M1 の quarantine 解除区分（`FLW-DSN-015`）は `confirmed-done`（intent・CAS receipt・現在 ref・object・fencing 一致）と `no-effect`（snapshot 不変、対象全体の副作用不存在）を**別区分として証跡で識別**していた。両者を分ける必要があるのは、前者なら operation は完了しており再実行してはならず、後者なら未実行なので新 plan で再実行してよいからである。\n\n§6 が追加した worktree 用区分には `confirmed-done` に相当するものが**無い**。そして `worktree-no-effect` の必須証跡は『dir / registry entry / local-ref のいずれも不在。**instance nonce も不在**』である。ここで問題は、`worktree.discard` / `worktree.finish` の**期待効果そのものが「三者の不在化」**だという点にある。discard が最後まで成功した後の観測は (dir=0, registry=0, ref=0) であり、instance nonce は registry entry 配下の owner-only file に保存される（§5）ため registry entry ごと消える。つまり **discard 成功後の観測と『discard がまったく実行されなかった』観測は完全に一致する。**\n\n§6 は `worktree-no-effect` を『evaluation-reviewer 承認で可』とし、解除後は §4 の単回 capability と新 operation ID で mutation を許す。しかし「実は成功していた」ケースをここへ落とすと、レビュアは『まだ削除されていない』と読み、新しい discard plan を承認する。その間に同じ work-id で worktree が作り直されていれば（`M2-FLT-027` が扱うまさにその状況）、新 plan は**新しい instance を削除対象にする**。§5 の create 時 nonce は「同一 operation の apply 直前 CAS」を守るが、この経路は**新しい plan・新しい operation ID・新しい nonce** で来るため CAS では止まらない。SYN-004 が quarantine 解除経路から再侵入する。\n\n本書 §6 が M1 の 5 区分のうち `confirmed-done` と `abandoned-with-compensation` を worktree へどう写像するかを一言も述べていないことが根本原因である（本文は『worktree 用**3区分**を追加する』と書き、表は 4 行あり、§15 は『worktree 用**4区分**』と書く。数の自己申告も一致していない）。",
      "recommendation": "(1) `worktree-confirmed-done` を独立区分として新設し、必須証跡を『intent の `expected_effect_digest` と一致する不在化 receipt、削除対象 manifest の全 entry の不在、fencing token 一致、hash-chain 上の該当 operation ID の完了 entry』とする。receipt は §4 のとおり common-dir 配下（worktree 実体側ではない）へ置かれるため、discard 後も残り識別に使える。(2) `worktree-no-effect` の必須証跡へ『当該 operation ID の receipt が **存在しない**こと』を追加し、成功と無効果を証跡で分ける。(3) M1 の 5 区分それぞれについて worktree 版での扱い（写像する／適用外）を §6 に明記する。(4) fixture へ『discard 完了後に quarantine 解除を要求』を追加し、期待結果を `worktree-confirmed-done` への一意確定＋新 plan 生成 0 とする。"
    },
    {
      "id": "DIN-005",
      "severity": "major",
      "title": "worktree-registry-stale の必須証跡「非存在証明」は §8 が ABA 節で自ら否定した否定的主張であり、区分が原理的に空になる",
      "detail": "§6 は `worktree-registry-stale` を独立区分にした理由を『外部要因で実体だけが消えるのが最も起きやすい残留形であり、これを `worktree-unresolved` に落とすと**回復可能な状態が恒久 quarantine に固定される**』と正しく説明する。ところがその必須証跡は『entry の `gitdir` が指す path が**存在しないことを証明できる**』であり、判定には『「`gitdir` が指す path の**非存在証明**」を必須とし、単に見えないだけ（mount 未接続・権限不足）と区別できない場合は `worktree-unresolved` とする』としている。\n\nこれは同じ文書の §8「ABA 検出の3経路」が確立した認識論と矛盾する。§8 は『ABA 検出が必要とするのは「T0 から T1 の間に更新が1件も無い」という**否定的な主張**だが、Activity API が提供できるのは「これらの更新が記録されている」という**肯定的な主張**だけである』『**観測遅延の上限は実験で確定できない**（速いことは示せても、遅くならないことは示せない）』と結論し、そのうえで capability があっても人間承認を省略しない 3 経路へ再構成した。**「見えない」から「存在しない」を導けないという同じ論理が、filesystem に対してこそ強く当てはまる**（mount 未接続、autofs、権限、ネットワーク FS の一時障害、case-insensitive FS 上の探索失敗）。\n\nそして §6 には ABA 側で採った緩和策（3 経路化・証跡の質の明示・不在を主張しない承認要求）が無い。『区別できない場合は unresolved』という規定を厳密に適用すると、非存在を積極的に証明する手段が存在しない以上、**`worktree-registry-stale` へ到達できるケースは事実上ゼロになり、§6 がこの区分を作った目的（最頻ケースを恒久 quarantine から救う）が達成されない。** 逆に実装が緩く判定すれば、mount 未接続の worktree の registry entry を『stale』として除去し、mount 復旧後に registry の無い実体（DIN-001 の (1,0,1) 状態）を作り出す。\n\n併せて `worktree-no-effect` の『instance nonce も不在』、`worktree-residue-retained` の『registry entry と ref は不在』も同型の否定的主張であり、同じ問題を持つ。",
      "recommendation": "§8 の 3 経路と同じ構造を §6 へ適用する。すなわち `worktree-registry-stale` の判定を (A) 親 directory を列挙して当該 entry 名が無いことを**肯定的に**確認でき、かつ親 directory 自体が承認済み root 配下で読み取り可能、(B) 親が読めるが判定に必要な capability が無い、(C) 親が読めない（mount 未接続・権限不足）の 3 経路へ分け、A のみを『evaluation-reviewer 承認で可』、B / C は『不在を主張しない旨を明示した承認要求』とする。あわせて『非存在証明』という語を文書から削り、実際に取得できる肯定的証跡（親 directory の entry 列挙結果とその snapshot digest）で記述し直す。"
    },
    {
      "id": "DIN-006",
      "severity": "major",
      "title": "create / resume の canonical guard key が「まだ存在しない path」に対して定義されておらず、SYN-002 が guard key 側で再発する",
      "detail": "§3 は `worktree-dir` の canonical key を『canonical common-dir identity ＋ worktree の canonical path の digest』とし、『canonical path は digest 化し、symlink・相対 path・case 差・別 clone を正規化して同一 worktree へ収束させる』と規定する。しかし **`worktree.create` の時点で対象 path は必ず存在しない**（§3 の case 感度節が『worktree-dir は create 時に必ず不在』と自ら認めている）。\n\n存在しない path に対して symlink 解決を含む canonicalization は定義できない。したがって create の guard key は『承認時に人間が読んだ path 文字列』を何らかの規則で正規化した値にならざるを得ないが、その規則（どこまで祖先を解決するか、末端の未解決成分をどう扱うか、`..` を字句的に潰すのか実体的に解決するのか）が §3 に無い。帰結は 2 つある。\n\n1. **同一実体を指す異なる文字列が別 key になる。** 祖先に symlink がある場合、`/approved-root/wt-a`（`approved-root` が `/mnt/x` への symlink）と `/mnt/x/wt-a` は、末端が不在でも祖先までは解決できるため実装次第で一致も不一致もしうる。不一致なら**同一 directory への並行 create が 2 本とも guard を取れる**。これは §3 が『raw path を key にしない』とした目的そのものの喪失であり、§4 で塞いだはずの `SYN-002`（承認 → guard 取得 → apply の間の親 symlink 差し替え）が、承認側ではなく **guard key 導出側**で再発する。\n2. `M2-FLT-006`（別 clone・別 mount から同一 worktree 要求 → 同一 guard へ収束）は、対象が既存の resume 系ケースなら通るが、**create（不在 path）では期待結果を判定する基準が設計に無い**。\n\n§9 の path 系 fixture（`M2-FLT-007` の escape 4 変種、`M2-FLT-008` の root symlink 差し替え、`M2-FLT-009` の不在 path の case 差）はいずれも『拒否／再承認／BLOCKED』を期待結果とする**安全側の検査**であり、『**不在 path に対する canonical key 導出が決定的である**』ことを検査する fixture は存在しない。`parent_dir_identity` と `nonexistence_digest` は capability 署名対象として apply 直前の再照合に使われるが、**guard key の導出そのものには関与しない**ため、key が割れていれば直列化は既に失われている。",
      "recommendation": "§3 へ『不在 path の canonical 化アルゴリズム』を明記する。推奨は『**存在する最も近い祖先まで実体解決（realpath）し、そこから先の未解決成分は字句的に正規化（`.` 除去・`..` を字句的に解決・case 感度は §3 の測定値で折り畳み）して連結する**』（§3 が case 感度判定で既に採った祖先遡り方式と同じ構造）。あわせて key の構成要素へ『解決に用いた祖先の stable file identity（dev+ino）』を含め、祖先の symlink 差し替えで key が変わることを保証する。fixture へ『同一実体を指す 4 通りの不在 path 表記（祖先 symlink 経由・相対・`..` 混在・case 差）から create を要求 → 4 件とも同一 guard key へ収束し、同時実行は最大 1 件』を追加し M2-1 へ割り当てる。"
    },
    {
      "id": "DIN-007",
      "severity": "major",
      "title": "create / resume の step 契約が存在しないのに PARTIAL / reconcile-only が割り当てられており、completed / remaining の一意化が実装不能",
      "detail": "§8 は『**「前進再開」の経路**』として PARTIAL を『reconcile で完了/残 step を確定 → 残 step だけの新 plan → 人間承認 → 新 operation ID で apply』と定義し、その前提として『**step 名を閉集合とし、`FLW-DSN-012` の `completed_steps` / `remaining_steps` へ写像する**』と規定する。ところが §8「step 契約」節が閉集合を定義しているのは `worktree.finish`（8 step）と `worktree.discard`（5 step）だけで、**`worktree.create` と `worktree.resume` の step 閉集合は存在しない**。\n\nそれにもかかわらず recovery matrix は `worktree.create | registry 公開後・双方向不一致 | PARTIAL | reconcile-only` を規定し、`M2-FLT-016` は『registry 公開の各点（temp / fsync / rename / dir fsync）で crash → 双方向一致の照合で `DONE` / `STALE` / `INDETERMINATE` へ**一意化**』を要求する。step 閉集合が無ければ `completed_steps` / `remaining_steps` を機械的に確定できず、PARTIAL の contract を満たせない。create は少なくとも `create-worktree-dir` / `write-registry-entry` / `create-local-ref` / `set-HEAD` / `publish-instance-nonce` に相当する複数の副作用を持つため、これは省略可能な単純 operation ではない。\n\n加えて同じ節の『**全 step がちょうど1つの mutation target に対応する**（残存 target を一意化するため）』という規定は、示された step 契約と**自己矛盾**している。finish の 8 step のうち `verify-pr-merge` / `verify-target-oid` / `verify-reachability` / `report-remote-candidate` の 4 つは本書自身が read-only と認めており（`report-remote-candidate` は明示的に『read-only の step であり remote ref 削除を含まない』）、mutation target を持たない。discard の `freeze-manifest` / `verify-manifest-scope` も同様である。つまり 13 step 中 6 step が規定に反する。この規定を機械検査すれば必ず FAIL するか、検査自体が実装されず沈黙する。\n\nさらに `FLW-DSN-013` の `REC-WORKTREE-CREATE` は同じ fault に対して『全一致なら `DONE`、**一部一致は `BLOCKED`**』と規定しており、本書の `PARTIAL` / `reconcile-only` と食い違う（DIN-010 参照）。同一 fault に 2 つの異なる正が存在する。",
      "recommendation": "(1) §8 の step 契約へ `worktree.create` / `worktree.resume` の step 閉集合を追加する。順序は §8 が discard / finish で採った原則（durability commit point ＝ registry entry の atomic 公開時点を正とする）と整合させ、create では逆順（dir 作成 → local-ref 作成 → **registry entry 公開を最後**）にすべきか否かを明示的に判断して根拠を書く。(2) 『全 step がちょうど1つの mutation target に対応する』を『**mutation step は**ちょうど1つの mutation target に対応し、verify / report step は mutation target を持たない read-only step として別に分類する』へ改め、step を `mutation` / `verify` の 2 種別に型付けする。(3) `M2-FLT-016` の期待結果へ『`completed_steps` / `remaining_steps` が create の step 閉集合の値のみで構成される』を追加する。"
    },
    {
      "id": "DIN-008",
      "severity": "major",
      "title": "状態 enum の判定述語（決定表）が本書に無く、三者照合は値集合しか見ないため「値は一致するが意味が乖離する」経路が塞がっていない",
      "detail": "§2 は『**閉集合（本書を唯一の正とする）**』と宣言し、`FLW-DSN-006` / `FLW-DSN-012` 側を『本表を参照する記述へ改める（GP-018）』とした。しかし §2 の表が持つのは namespace / field / 値の性質 / closed enum の 4 列だけで、**各値の判定条件（どの証跡が揃えばその値になるか）は 1 行も無い**。判定条件の正は依然として `FLW-DSN-006` の「audit分類」表にあり、実測でその表は今も `active-clean` / `active-dirty` / `pr-open` / `merged-exact` / `remote-advanced` / `worktree-mismatch` / `orphan` / `failed-retained` の**小文字 kebab 8 値**のままである（`FLW-DSN-006` v1.1、`ABSENT` に相当する行も無いため §2 の 9 値と件数も一致しない）。\n\nこれは §2 が塞いだつもりの穴が形を変えて残っていることを意味する。`FLW-REV-011:SYN-001` の本質は『どちらを見ても正が確定しない』であり、いま起きているのは『**値の正は §2、意味の正は FLW-DSN-006、両者は表記も件数も一致しない**』という分裂である。そして §2 が導入した三者照合テストは『設計の閉集合・schema の enum・実装の定数』という**値集合**の双方向照合であり、値と判定述語の対応は検査対象に入らない。したがって実装が `ACTIVE_CLEAN` という正しい値を、誤った条件（例: registry 双方向一致を確認せずに返す）で返しても、三者照合は green のまま沈黙する。§2 自身が `guard_identity_kind` について『外した namespace は…設計と実装の乖離が沈黙する』と警戒した経路と同型である。\n\nさらに §15 は GP-018 への応答として『§2 ＋ **実ファイル更新**（`FLW-DSN-006` 1.1 / `FLW-DSN-012` 1.2 / `FLW-DSN-015` 1.1 を同じ変更セットで）』と書くが、実測では `FLW-DSN-006` の version は 1.1 へ上がっている一方、GP-018 が求めた『状態語を表記規則へ揃え』は audit 分類表に未適用である（create / resume 節には enum 対応表が追加されているが、audit 分類表は手つかず）。**GP への応答が実ファイルの状態と食い違っており、§15 の逐語併記方式でも取り違えが素通りしている。**",
      "recommendation": "(1) §2 へ `worktree_state` / `branch_audit_state` / `work_unit_state` の**決定表**（値 × 必須証跡 × 排他条件 × 許可 operation）を移設し、`FLW-DSN-006` の audit 分類表は §2 を参照する記述へ置き換える（値と意味の正を同じ場所に置く）。決定表には DIN-002 が指摘した同時成立時の優先順位を含める。(2) 三者照合テストの対象へ『各値に少なくとも1つの判定述語が定義されていること』と『決定表の値集合 ＝ 閉集合』を加え、値だけの照合に留めない。(3) `FLW-DSN-006` の audit 分類表を実際に大文字スネークへ変換し、`ABSENT` 行を追加して 9 値に揃える（GP-018 の応答を事実に一致させる）。"
    },
    {
      "id": "DIN-009",
      "severity": "major",
      "title": "要件層（FLW-FR-007）が返す値は小文字のままで設計 §2 と食い違い、三者照合の対象が要件を含まないため恒久的に沈黙する",
      "detail": "§2 は `branch_audit_state` を `ACTIVE, MERGED_EXACT, REMOTE_ADVANCED, WORKTREE_IN_USE, ORPHAN` の大文字スネークで定義し、『`FLW-FR-007` の受入基準はこの一本化に合わせて改訂する』と書く。実測すると `FLW-FR-007` は 1.1（2026-08-12）へ改訂済みだが、改訂されたのは `indeterminate` の削除だけで、受入基準は依然として『bitz-flow は `active`、`merged-exact`、`remote-advanced`、`worktree-in-use`、`orphan` のいずれかを返すこと SHALL』と**小文字 kebab のまま**である。\n\n`FLW-FR-007` は EARS 要件であり `verification_method` を持つ検証対象である。つまり実装が §2 に従って `ACTIVE` を返すと**要件の受入基準に違反し**、要件に従って `active` を返すと**§2 の閉集合と schema に違反する**。どちらへ倒しても片方が FAIL する矛盾が、Design Gate を通る前提の要件層に埋まっている。\n\n同じ要件層の中でも表記が割れている。`FLW-CON-006` 1.3 は『削除対象 branch が `REMOTE_ADVANCED` に分類される THEN plan を生成せず BLOCKED』と**大文字**で書いており、`FLW-FR-007` の `remote-advanced` と同じ概念を別表記で参照している。同じ 2026-08-12 の裁定（SI-FLW-044 / SI-FLW-046）で同時に改訂された 2 つの要件が、同じ enum について異なる表記を採っている。\n\n決定的なのは、§2 が導入した三者照合テストの対象が『**設計の閉集合・schema の enum・実装の定数**』の 3 者であり、**要件が含まれていない**ことである。したがってこの乖離は機械検証で永久に検出されない。§2 が『外した namespace は…沈黙する』と警戒したのと同じ理由で、外した**層**も沈黙する。",
      "recommendation": "(1) `FLW-FR-007` の受入基準の値を `ACTIVE` / `MERGED_EXACT` / `REMOTE_ADVANCED` / `WORKTREE_IN_USE` / `ORPHAN` へ改め、version を 1.2 へ上げる。§2 の宣言（『受入基準はこの一本化に合わせて改訂する』）を実際に履行する。(2) 三者照合を**四者照合**へ拡張し、要件本文中のバッククォート囲み enum 値も照合対象に含める（`spec_inspect` が要件を機械読解できる形式で保持しているため実現可能）。少なくとも §12 の M2 出口条件へ『要件本文の enum 値が §2 の閉集合と一致すること』を追加する。(3) `FLW-CON-006` と `FLW-FR-007` の表記統一を同一変更セットで行う。"
    },
    {
      "id": "DIN-010",
      "severity": "major",
      "title": "recovery class 識別子が 2 系統に分裂し、FLW-DSN-013 の worktree 4 クラスは §8 と矛盾する挙動を規定したまま残っている",
      "detail": "本書 §1 の operation catalog は recovery class を `REC-WT-CREATE` / `REC-WT-RESUME` / `REC-WT-FINISH` / `REC-WT-DISCARD` / `REC-RM-DELETE` と採番する。しかし `FLW-DSN-012.md:51-55` は同じ operation に `REC-REMOTE-DELETE` / `REC-WORKTREE-CREATE` / `REC-WORKTREE-FINISH` / `REC-WORKTREE-DISCARD` を割り当て、`FLW-DSN-013.md:42-45` はその 4 クラスの**実際の回復手順表**を持つ。本書は 4 文書（`FLW-DSN-006` / `012` / `013` / `015`）を『結合する規範設計』と位置づけ『矛盾する場合は本書の M2 固有表を優先』とするが、**識別子を勝手に改名したうえで旧識別子の定義表を放置している**。`REC-WT-RESUME` に至っては `FLW-DSN-013` に対応行が無い（`REC-WORKTREE-CREATE` が create/resume 兼用）ため、resume の回復手順の正が存在しない。§8 冒頭が『未登録 tuple、未知 field、code/cause 矛盾は `human-stop` へ fail-closed』と規定する以上、識別子の分裂は実装時に全 resume 回復を human-stop へ倒す。\n\n識別子だけでなく**中身も矛盾**している。\n\n| fault | `FLW-DSN-013` の規定 | 本書 §8 の規定 |\n|---|---|---|\n| create 応答喪失・一部一致 | `BLOCKED` | `PARTIAL` / `reconcile-only` |\n| finish 応答喪失 | 『消えた段階を completed_steps へ入れ、**残存 branch だけ再開**』 | `PARTIAL` / `reconcile-only`。**残 step の自動 apply を禁止** |\n| discard 応答喪失・列挙 target だけ不存在 | `DONE` | `worktree-no-effect` との識別問題（DIN-004） |\n\n特に finish の『残存 branch だけ再開』は**自動前進**であり、本書 §8 が『`FLW-DSN-006` の「再実行で前進再開する」と `FLW-DSN-015` の `reconcile-only` を**後者へ統一**する』と明示的に裁定した内容の反例が `FLW-DSN-013` に残っている。にもかかわらず §14「影響範囲・ロールバック」は変更対象を `guard_identity_kind` / `FLW-NFR-007` / `FLW-CON-006` の 3 点に限定し、『M1 operation の guard 導出規則・**recovery matrix**・qualification プロトコルは変更せず追加のみである』と述べる。`FLW-DSN-013` の worktree 4 クラスの意味を変えている以上、この記述は事実と異なる。",
      "recommendation": "(1) recovery class 識別子を 1 系統に統一する。既存 3 文書に登録済みの `REC-WORKTREE-*` / `REC-REMOTE-DELETE` を正とし、本書 §1 の表を改めるのが変更量・破壊性ともに最小である。改名を採るなら `FLW-DSN-012` / `FLW-DSN-013` を同一変更セットで更新する。(2) `REC-WORKTREE-RESUME` を新設し `FLW-DSN-013` へ回復手順行を追加する。(3) `FLW-DSN-013` の `REC-WORKTREE-CREATE`（一部一致 → BLOCKED）と `REC-WORKTREE-FINISH`（残存 branch だけ再開）を §8 の裁定へ合わせて改訂し、§14 の影響範囲表へ `FLW-DSN-013` の行を追加する。(4) fixture へ『recovery class 識別子の集合が全設計文書で一致すること』の静的検査を加え、`M2-FLT-036`（未登録 recovery tuple）の検査対象へ含める。"
    },
    {
      "id": "DIN-011",
      "severity": "major",
      "title": "manifest digest が内容 hash を持たず mtime 精度に依存し、nonexistence_digest の導出規則も未定義で、いずれも capability 縮退表に無い",
      "detail": "§5 の `worktree-dir` CAS 相当は、削除対象 manifest の各 entry を `(相対 path, type, dev+ino, size, mtime_ns)` とし、その全体 digest を apply 直前に再計算する。**ファイル内容の digest を含めない**選択の根拠は書かれていない。この構成では次を検出できない。\n\n- **同一 size・同一 mtime での内容書き換え**。`mtime_ns` という field 名は ns 精度を示唆するが、実際の精度は filesystem 依存であり、exFAT / FAT は 2 秒、HFS+ は 1 秒、ext4 も inode 形式によっては 1 秒精度である。M2 は Linux / macOS / Windows の 3 platform で confirmation を要求しており（§12）、低精度 FS は現実的な対象である。M1 が `git.stage` に index digest CAS、`git.commit` に expected-OID CAS という**内容ベースの CAS** を課したのに対し、worktree-dir だけが**メタデータベース**の弱い述語に落ちている。§5 は『これにより `worktree-dir` の防御水準は `git.delete-remote-branch` **と同等以上**になる』と主張するが、比較対象の `git.delete-remote-branch` は `SI-FLW-044` により expected-OID（＝内容 hash）CAS へ厳格化された。同等以上という自己申告は成立していない。\n- **時刻の巻き戻し**（NTP 補正、VM snapshot 復元、手動 `touch -d`）による mtime 一致の偽陽性。\n\nそして §5 の **capability 縮退表**が挙げる縮退条件は『stable file identity（`dev+ino` 相当）の取得』『dirfd 相対削除』『承認済み root への owner-only 書き込み』『root の case 感度判定』の 4 つで、**mtime 分解能は入っていない**。すなわち 2 秒精度 FS 上でも `worktree.discard` は `UNSUPPORTED` へ落ちず、弱い CAS のまま破壊操作を実行する。\n\n併せて §4 の署名対象 `nonexistence_digest`（create 専用、『対象が存在しないことの証明 digest』）は**導出規則が定義されていない**。非存在それ自体は hash 化できないため、実際には親 directory の entry 一覧など何らかの肯定的観測を hash する必要があるが、その対象が親 dir 全体なら**無関係な worktree の作成で digest が変わり false STALE が頻発**し、対象 entry 名だけなら『存在しない』の表現方法が未定義である。`M2-FLT-012`（承認後・apply 前に別 process が先行占有 → `nonexistence_digest` 不一致で `STALE`）は、この導出規則なしには実装も検証もできない。",
      "recommendation": "(1) manifest entry へ**内容 digest** を追加する（通常ファイルは content sha256、symlink は link target、directory は子 entry 名集合の digest）。大規模 worktree のコストが問題なら、`size + mtime` が一致した entry のみ content digest を省略する最適化を明示的な**縮退**として記述し、その条件下で防御水準が下がることを認める。(2) capability 縮退表へ『mtime 分解能が 1ms 未満であること』を必要 capability として追加し、満たさない platform では `worktree.discard` を `UNSUPPORTED` にするか content digest を必須化する。(3) §4 へ `nonexistence_digest` の導出規則を明記する（推奨: 親 directory の canonical identity ＋ 対象 entry 名 ＋ 対象 path への `lstat` が ENOENT を返した事実を含む署名可能な観測記録）。(4) fixture へ『同一 size・同一 mtime での内容差し替え後に discard を apply』を追加し M2-5 へ割り当てる。"
    },
    {
      "id": "DIN-012",
      "severity": "major",
      "title": "work_unit_state は 12 値の列挙のみで遷移関係が無く、worktree_state 9 値のうち 3 値は FLW-DSN-012 の写像表から到達できない",
      "detail": "§2 は `work_unit_state` を 12 値へ回復したことを GP-001 への応答としているが、提示されているのは**値の列挙だけ**で、遷移関係（どの状態からどの状態へ、どの operation で遷移するか）は本書にも `FLW-DSN-012` にも無い。`FLW-DSN-012` の写像表は『正規 WorkUnit state × worktree state × PR state × 許可 action』の対応表であって遷移グラフではない。遷移グラフが無いため、到達不能状態・出口の無い状態（吸い込み）の機械検査ができず、§8 が規定する『NEXT は許可グラフの**到達可能性**を検査する』も WorkUnit 層では実行できない。実際に次の欠落が確認できる。\n\n- **worktree_state の 3 値が写像表から到達不能**。`FLW-DSN-012` の写像表が worktree state 列で使う値は absent / active-clean / active-dirty / pr-open / merged-exact / orphan の 6 種であり、§2 の 9 値のうち `REMOTE_ADVANCED` / `WORKTREE_MISMATCH` / `FAILED_RETAINED` に対応する WorkUnit 状態が存在しない。`REMOTE_ADVANCED` は `FLW-CON-006` が『plan を生成せず BLOCKED』とする重要な安全状態であり、`WORKTREE_MISMATCH` は `M2-FLT-018` の期待結果に直結する。\n- **`FAILED_RETAINED` は §2 自身の原則に反する**。§2 は『audit 分類は**外部事実から再構成できる観測状態に限る**』とし、由来情報を含む `registered-active` を除いた。ところが `FAILED_RETAINED` の条件は `FLW-DSN-006` で『**失敗裁定**、変更を保持』であり、これは人間の裁定という**履歴情報**である。外部から観測すれば `ACTIVE_DIRTY` と区別できない。`registered-active` を除いた根拠をそのまま適用すれば `FAILED_RETAINED` も `worktree_state` から除かれるべきだが、原則が一貫適用されていない。\n- **終端状態の後に破壊操作が走る**。`CLEANED` / `DISCARDED` は終端と明記されるが、§8 の finish step は `report-remote-candidate` で終わり、その後 `git.delete-remote-branch` が別 operation として実行される。remote ref がまだ存在する状態を表す WorkUnit 状態が無く、終端到達後に destructive operation が走る構造になっている。`work_unit_state` と operation catalog の対応が閉じていない。\n- **`AUDITED` を前提としない finish の検査が無い**。§2 は『`audited` は `worktree.finish` の唯一の許可前提』と書くが、§8 の recovery matrix にも §9 の fixture にも『`AUDITED` でない WorkUnit へ finish を要求』する行が無い。",
      "recommendation": "(1) §2 へ `work_unit_state` の**遷移表**（from / to / trigger operation / precondition）を追加し、到達不能状態と吸い込み状態が 0 であることを三者照合テストと同じ機械検査の対象にする。(2) `REMOTE_ADVANCED` / `WORKTREE_MISMATCH` / `FAILED_RETAINED` に対応する WorkUnit 状態を `FLW-DSN-012` の写像表へ追加するか、対応が不要である根拠（例: これらは operation 結果の分類であって WorkUnit 状態ではない）を明記する。(3) `FAILED_RETAINED` を `worktree_state` から除いて `work_unit_state` 側のみに残すか、残す場合は『由来情報を含む状態は除く』という §2 の原則の例外として理由を書く（§2 が `trial_kind` の反例を隠さず記載したのと同じ扱い）。(4) fixture へ『`AUDITED` でない WorkUnit へ finish を要求 → `BLOCKED`』を追加する。"
    },
    {
      "id": "DIN-013",
      "severity": "minor",
      "title": "cause namespace は三者照合の対象と宣言されながら設計側の閉集合が本書に無く、11 namespace 中 1 つで照合が成立しない",
      "detail": "§2 の namespace 表は 11 行あり、最終行の `cause` は『値の正は `FLW-DSN-005` の許可語彙（本書で変更しない）』とされている。そのうえで『三者照合テストの対象は**本表の全 namespace** とする。`cause` は値の正を `FLW-DSN-005` に置いたままだが、照合対象から外さない — 外した namespace は `guard_identity_kind` と同じ経路で腐るためである』と規定する。\n\n意図は正しいが、実行可能性が担保されていない。§2 は同じ節で『実装定数は…設計文書から**機械抽出できる形式（本書の表）を正**とし、テストが差分を出す』と規定しており、抽出元は本書の表である。ところが `cause` 行の closed enum 列には値が 1 つも書かれていないため、**設計側の入力が空**になる。空集合との双方向照合は『schema ⊆ 設計』側が必ず FAIL するか、実装が `cause` を暗黙にスキップして沈黙するかのどちらかで、後者はまさに §2 が警戒した `guard_identity_kind` の経路そのものである。\n\n同様に、§2 の『多重語一覧は schema から機械導出する』導出規則も『全 namespace の closed enum を集め、2つ以上の namespace に現れる値を列挙する』としているが、`cause` の値が本書に無いため導出に参加できない。`cause` は小文字 kebab であり `guard_identity_kind`（`index` / `local-ref` / `remote-ref` …）と同じ表記空間にあるため、衝突が起きても検出されない。\n\n（検算した範囲では、§2 の多重語表 13 行は `cause` を除く 10 namespace から導出した結果と**完全に一致**しており、`PLANNED` から `FAILED_RETAINED` まで漏れは無かった。問題は表の内容ではなく、導出への入力が 1 namespace 分欠けていることにある。）",
      "recommendation": "`cause` について、(a) `FLW-DSN-005` の許可語彙を §2 の表へ転記して本書を唯一の正にする、(b) 三者照合テストの設計側入力を『本書の表 ＋ `FLW-DSN-005` の許可語彙表』の 2 ファイルからの機械抽出とし、抽出元を明記する、のいずれかを選ぶ。どちらも採らないなら『`cause` は照合対象外』と明記して沈黙を意図的なものにする（現状の『対象とするが入力が無い』が最も悪い）。あわせて多重語導出に `cause` を含め、`guard_identity_kind` との表記空間衝突を検査する。"
    },
    {
      "id": "DIN-014",
      "severity": "info",
      "title": "検算して正しかった点 — 多重語表・fixture 区分割当・instance nonce・registry-before-dir の順序根拠",
      "detail": "本レビューは自己申告を検証する方針で数え直したため、成立していた主張も記録する。\n\n1. **§2 の多重語表は完全である。** 10 namespace（`cause` を除く）の closed enum を集めて 2 つ以上の namespace に現れる値を機械的に導出すると、`PLANNED` / `DONE` / `PARTIAL` / `STALE` / `RECONCILING` / `QUARANTINED` / `BLOCKED` / `PASS` / `FAIL` / `ACTIVE` / `MERGED_EXACT` / `REMOTE_ADVANCED` / `ORPHAN` / `FAILED_RETAINED` の 14 語となり、§2 の 13 行（`PASS` / `FAIL` を 1 行に併記）と完全一致する。`FLW-REV-011:SYN-014` の『4/10 漏れ』は解消されている。\n2. **§9 の fixture 区分割当に未割当・重複が無い。** M2-1: 001〜009（9 件）、M2-2: 010〜015（6 件）、M2-3: 016〜023（8 件）、M2-4: 045〜047（3 件）、M2-5: 024〜036（13 件）、M2-6: 037〜044 ＋ 048 ＋ 049（10 件）＝ 49 件で、§11 の完了条件と §12 の出口条件（001〜049）に一致する。§9 の完了条件『ちょうど1つの実装区分へ割当・未割当と重複割当 0』は成立している（表の行順序が 037, 038, 048, 049, 039… と乱れている点のみ可読性の問題）。\n3. **§5 の create 時 nonce による instance 識別は SYN-004 の同一 operation 内経路を確実に閉じる。** work-id が決定的でも nonce は単回値なので、同じ path・同じ work-id で作り直された worktree は必ず別 instance になる。§3 が guard key から instance を外し precondition へ移した判断（v1.1 の是正）も、直列化と同一性照合を分ける正しい分解である。\n4. **§8 の `remove-registry-entry` を `remove-worktree-dir` より先に置く判断は、durability commit point を registry の atomic 公開時点とする M1 の規定と整合しており、方向としては正しい**（ただし DIN-001 のとおり、その帰結を受け止める §6 の区分が欠けている）。\n5. **§8 の ABA 3 経路は認識論的に正しい。** 『肯定的主張しか得られない観測から否定的主張を導かない』という原則と、capability の有無が承認要否を変えないという結論は妥当で、`FLW-CON-006` 1.3 の受入基準とも一致する。本レビューの DIN-005 は、この正しい原則が §6 へ適用されていないことを指摘するものである。",
      "recommendation": "維持する。特に (5) の認識論を §6 の解除区分・§5 の非存在系証跡へも横展開すれば、DIN-004 / DIN-005 は同じ枠組みで解消できる。§9 の表は ID 昇順へ並べ替えることを推奨する。"
    }
  ],
  "gp_verification": []
}
