# CLAUDE.md

共通ルールはすべて @AGENTS.md を正とする。

## Claude Code 固有

- 危険操作の機械的ブロックは `.claude/settings.json` の permissions で強制している
  （AGENTS.md のガードレール節と対応。緩めるときは両方を見直す）

## 委譲

司令塔（ユーザーが起動したモデル）は計画・タスク分解・検収・最終判断に徹し、
実作業はサブエージェントへ委譲する。委譲先は役割で選び、モデルは `.claude/agents/*.md` の
frontmatter だけで指定する（本ファイルにモデル名を書かない）。

| 役割 | 委譲先 |
|---|---|
| 手順と検証方法が明確な実装・修正・試験実行 | `worker` |
| Step 完了・Gate 判定・「解消済み」主張の独立検分 | `reviewer` |
| コードベースの広範な探索 | 組み込み `Explore` |

- 1 ファイルの軽微な作業は委譲せず司令塔が行う（往復コストが節約分を上回る）
- 委譲時は期待する成果物・報告形式・検証方法を指示に含める
- 検収は司令塔が行う。worker の報告を信用せず、fixture に現れない欠陥を probe・
  `PYTHONHASHSEED` の変更・実 Git repository での抜き取りで確かめる
- 仕様解釈が必要な点は worker に決めさせず、司令塔がユーザーへ上げる
- サブエージェントの同時起動は 5〜6 体まで
